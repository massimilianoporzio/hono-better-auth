{
  "name": "hono-better-auth",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "db:up": "podman compose up -d"
  },
  "dependencies": {
    "hono": "^4.8.5"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
